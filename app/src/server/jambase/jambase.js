const request = require('request'),
    logger = require(`app/src/server/common/util/logger`).logger('jambase');

function jambase (bot, data) {
    request(``, function (error, response, body) {
    });

}

module.exports = jambase;
